package com.test.answer;

/**
 * Deadlock
 */
public class Test3 {
//    When method runs, it's extremely likely that both threads will block when they
//    attempt to invoke bowBack. Neither block will ever end, because each thread
//    is waiting for the other to exit bow.
}
